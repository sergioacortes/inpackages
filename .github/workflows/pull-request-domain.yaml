name: Inpackages Domain - Pull request

on:
  pull_request:
    types: [ opened, synchronize, reopened, closed ]
    branches:
      - main
    paths:
      - 'packages/Inpackages.Domain/**'

jobs:
  setup:
    uses: ./.github/workflows/setup.yml
    with:
      gitversion: '6.3.0'